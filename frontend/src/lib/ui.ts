// Shared Tailwind class strings so form fields look consistent across the app.
export const inputClass =
  "border border-gray-200 rounded-xl px-4 py-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-primary/10 transition-colors";
export const selectClass = `${inputClass} cursor-pointer`;
export const labelClass = "text-sm font-semibold text-dark";
